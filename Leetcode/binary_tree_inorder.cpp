class Solution {
public:
    vector<int> inorderTraversal(TreeNode* root) {
        vector<int> nodes;
        if (!root) return nodes;
        
        stack<TreeNodes*> st;
        st
        // check left subtree
        while(!st.empty()){
            TreeNode* temp = st.top();
            st.pop();
            nodes.push_back(tp)
        }
        st.push(root);
        
    }
};